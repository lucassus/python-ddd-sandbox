# fastapi sandbox

## Preparing and running the app

* `make venv`
* `make install`
* `python -m todos.entrypoints.cli.seed`
* `make server-dev`

## Linting and testing

* `make lint`
* `make test`
* `make test-watch`
